# Pip

一个只给自己和几个朋友用的打卡应用。在日历上留点，想打就打——**不做提醒、不做倒计时、不做连击天数**。

## 文档

动手前读这三份，它们分工不同：

- [`docs/PRD.md`](docs/PRD.md) — **产品长什么样、怎么用、什么风格**。页面、交互、视觉规范、文案。要做什么以它为准
- [`docs/DESIGN.md`](docs/DESIGN.md) — **为什么这么设计**。技术决策与理由
- 本文 — **动手时的硬约束**，冲突时以本文为准

## 形态

- Web 应用（PWA），纯静态，无后端
- 托管在 GitHub Pages（public 仓库）
- 数据存浏览器 localStorage
- 通过「添加到主屏幕」当 App 用

## 约定

- 代码里统一用 `pip` 指代一个打卡标记（数据字段叫 `pips`，不叫 `marks`），与产品名保持一致
- 日期一律用**本地时区**拼 `YYYY-MM-DD`，**禁止使用 `toISOString()`**——它按 UTC 走，东八区晚 8 点后会记错一天
- 日历按周一起始，列偏移用 `(getDay() + 6) % 7`，不要直接拿 `getDay()` 当索引
- 存储层必须抽象成**返回 Promise 的异步接口**，界面不直接接触 localStorage
- 每条打卡记录带 `updated_at`，数据带全局 `version`，为将来合并/同步留路

## 不要做

- 提醒、通知、倒计时、连击
- 后端、账号、登录（当前阶段不需要）
- 引入框架或构建工具，除非有明确理由

## 踩过的坑

- 换域名 = 换 origin = 旧数据读不到。**域名一旦发出去就定死**
- iOS 上「添加到主屏幕」的应用有独立存储，和 Safari 里是两份数据，必须引导用户只用主屏幕图标
- `file://` 和 `localhost` 各自是独立 origin，本地测试的数据不会出现在线上
