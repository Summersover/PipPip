# Pip

一个只给自己和几个朋友用的打卡应用。在日历上留点，想打就打——**不做提醒、不做倒计时、不做连击天数**。

## 文档

动手前读这两份：

- [`docs/PRD.md`](docs/PRD.md) — **产品长什么样、怎么用、什么风格**。页面、交互、视觉规范、文案。要做什么以它为准
- [`docs/TECH.md`](docs/TECH.md) — **怎么实现**。文件结构、存储层接口、日期工具、渲染策略、PWA、导出导入、部署清单

本文是动手时的硬约束，与上面两份冲突时以本文为准。

## 形态

- Web 应用（PWA），纯静态，无后端
- 托管在 GitHub Pages（public 仓库）
- 数据存浏览器 localStorage
- 通过「添加到主屏幕」当 App 用

## 约定

- 产品名 **Pip** 已定死。已排除 Tally / Dotcal / Notch / Ledger / Marked（理由见 `docs/PRD.md` 第 1 节），不要重新提议改名
- **`template`** = 打卡模板（可打卡的东西），**`pip`** = 一次打卡记录。一个模板一天可以产生多条 pip
- pip 是**独立记录**（有 id、可单独改备注和删除），不是「模板在某天的状态」。数据字段叫 `pips`，不叫 `marks`
- 界面文案禁止出现「完成」「待办」「未完成」「打卡成功」这类措辞——本产品只有「记录」，没有「欠着什么」。说「删除记录」，不说「取消打卡」
- 日期一律用**本地时区**拼 `YYYY-MM-DD`，**禁止使用 `toISOString()`**——它按 UTC 输出，东八区**凌晨 00:00–07:59** 之间会把日期算成**前一天**（负偏移时区如纽约则是晚间 20:00–23:59 算成后一天）。错误窗口随偏移变化，`npm run test:tz` 会在五个时区下把它打印出来
- pip 的 `date`（本地日期字符串，用于按天分组）和 `at`（时间戳，用于当天内排序）**都要存**，不能只存一个
- 日历按周一起始，列偏移用 `(getDay() + 6) % 7`，不要直接拿 `getDay()` 当索引
- 日历格子里的点：**每个 pip 一个点**，最多两行共 8 个（5 + 3），超过则第二行末尾显示 `+N`。点区高度固定为两行，没有点也要占位（否则日期数字不齐）
- 存储层必须抽象成**返回 Promise 的异步接口**，界面不直接接触 localStorage
- 每条记录带 `updated_at`，数据带全局 `version`，为将来合并/同步留路
- 停用模板（`archived`）不删记录；只有真删除才连记录一起删，且必须二次确认
- **运行时依赖必须为零**。允许有开发依赖（目前只有 `typescript`，用于 JSDoc 类型检查，不进产物）
- 改完 `js/` 下的代码跑 `npm run check`（类型检查 + 5 个时区测试 + 校验 `sw.js` 生成区域）
- 相对 import **必须写扩展名**（`'./dates.js'`，不是 `'./dates'`），否则浏览器里直接 404

## 不要做

- 提醒、通知、倒计时、连击
- **任何「待办」「完成度」「计划」概念**——这是本产品最重要的边界，见 `docs/PRD.md` 第 2 节
- 后端、账号、登录（当前阶段不需要）
- 引入框架或打包器。当前是原生 ES 模块 + JSDoc 类型检查，理由和「什么情况下才改推荐」见 `docs/TECH.md` 1.2
- 手改 `sw.js` 里的 `CACHE` 和 `ASSETS`——它们由 `tools/stamp-sw.js` 生成
- 把派生数据（统计、计数）写进存储，一律读时计算（见 `docs/TECH.md` 6.5）

## 踩过的坑

- 换域名 = 换 origin = 旧数据读不到。**域名一旦发出去就定死**
- iOS 上「添加到主屏幕」的应用有独立存储，和 Safari 里是两份数据，必须引导用户只用主屏幕图标
- `file://` 和 `localhost` 各自是独立 origin，本地测试的数据不会出现在线上
- **Git Bash 下 `TZ=Asia/Shanghai node ...` 这种写法不生效**——`process.env.TZ` 会是 `undefined`，时区静默回落成系统值。多时区测试必须用 `spawnSync` 显式传 env，否则会假装在测五个时区、实际跑的是同一个
- Node 的测试运行器会自动抓取 `test-*.js`，所以 `tools/` 下的脚本别用这个名字（`check-tz.js` 而不是 `test-tz.js`）
- `npx tsc` 会装到一个**同名的废弃包**（`tsc@2.0.4`，不是 TypeScript）。要用 `npx tsc` 前先确认 `npm ls typescript`，或者直接用 `npm run typecheck`
